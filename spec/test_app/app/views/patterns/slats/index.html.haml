

%hr
%p.my-5

%h1 Slats

%h2.mb-4 Manual build
= ui.bootstrap :slats do
  = ui.bootstrap :slat_header do
    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do
        = ui.bootstrap :slat_item, slat_header: 'First Column'
        = ui.bootstrap :slat_item, slat_header: 'Second Column'
      = ui.bootstrap :slat_actions, slat_header: true

  = ui.bootstrap :slat_list do
    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do
        = ui.bootstrap :slat_item, heading: 'Karl Fergood live here at this column', caption: 'A caption about this contact'
        = ui.bootstrap :slat_item, :truncate do
          = ui.bootstrap :typeface, :truncate, body: 'aReallyLongStringForTruncationExampleIsPlacedHereAndBecauseItsGreatToStressTestItsEvenLonger!'


      = ui.bootstrap :slat_actions do
        = ui.bootstrap :slat_action, href: '#', body: 'Edit', icon: 'pencil'
        = ui.bootstrap :slat_action, :danger, href: '#', body: 'Delete', icon: 'trash-o', method: :delete, confirm: 'Are you sure you want to delete?'

    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do
        = ui.bootstrap :slat_item, heading: 'Karl Fergood - Link', href: '#nogo'
        = ui.bootstrap :slat_item, heading: '$5000', caption: 'An amount caption'

    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do
        = ui.bootstrap :slat_item, heading: 'Non-wide slat set on slat_actions (wide: false)'
        = ui.bootstrap :slat_item, heading: '$5000', caption: 'An amount caption'

      = ui.bootstrap :slat_actions, wide: false do
        = ui.bootstrap :slat_action, href: '#', body: 'Edit', icon: 'pencil'
        = ui.bootstrap :slat_action, :danger, href: '#', body: 'Delete', icon: 'trash-o', method: :delete, confirm: 'Are you sure you want to delete?'

.mt-5
%hr
.mb-5

%h3.mb-4 Non-wide slat rows (action dropdowns without text)

= ui.bootstrap :slats do
  = ui.bootstrap :slat_header do
    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do
        = ui.bootstrap :slat_item, slat_header: 'First Column'
        = ui.bootstrap :slat_item, slat_header: 'Second Column'
      = ui.bootstrap :slat_actions, slat_header: true, wide: false

  = ui.bootstrap :slat_list do
    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do

        = ui.bootstrap :slat_item, heading: 'Non-wide slat set on slat_actions (wide: false)'
        = ui.bootstrap :slat_item, heading: '$5000', caption: 'An amount caption'

      = ui.bootstrap :slat_actions, wide: false do
        = ui.bootstrap :slat_action, href: '#', body: 'Edit', icon: 'pencil'
        = ui.bootstrap :slat_action, :danger, href: '#', body: 'Delete', icon: 'trash-o', method: :delete, confirm: 'Are you sure you want to delete?'
    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do
        = ui.bootstrap :slat_item, heading: 'Non-wide slat set on slat_actions (wide: false)'
        = ui.bootstrap :slat_item, heading: '$5000', caption: 'An amount caption'

      = ui.bootstrap :slat_actions, wide: false do
        = ui.bootstrap :slat_action, href: '#', body: 'Edit', icon: 'pencil'
        = ui.bootstrap :slat_action, :danger, href: '#', body: 'Delete', icon: 'trash-o', method: :delete, confirm: 'Are you sure you want to delete?'


.mt-5
%hr
.mb-5

%h3.mb-4 Slats without action menus but with action column

= ui.bootstrap :slats do
  = ui.bootstrap :slat_header do
    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do
        = ui.bootstrap :slat_item, slat_header: 'First Column'
        = ui.bootstrap :slat_item, slat_header: 'Second Column'
      = ui.bootstrap :slat_actions, slat_header: true

  = ui.bootstrap :slat_list do
    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do

        = ui.bootstrap :slat_item, heading: 'Slat information'
        = ui.bootstrap :slat_item, heading: '$5000', caption: 'An amount caption'

      = ui.bootstrap :slat_actions, menu: false, icon: 'trash', body: 'Delete', href: '#nogo'
    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do

        = ui.bootstrap :slat_item, heading: 'Button version'
        = ui.bootstrap :slat_item, heading: '$5000', caption: 'An amount caption'

      = ui.bootstrap :slat_actions, menu: false, icon: 'trash', body: 'Button', href: '#nogo', button: true
    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do

        = ui.bootstrap :slat_item, heading: 'Slat information'
        = ui.bootstrap :slat_item, heading: '$5000', caption: 'An amount caption'

      = ui.bootstrap :slat_actions, :danger, menu: false, icon: 'trash', body: 'Delete', href: '#nogo', remote: true, method: :get

.mt-5
%hr
.mb-5

%h3.my-4 Slats without action menus but with action column, icon only (not wide)

= ui.bootstrap :slats do
  = ui.bootstrap :slat_header do
    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do
        = ui.bootstrap :slat_item, slat_header: 'First Column'
        = ui.bootstrap :slat_item, slat_header: 'Second Column'
      = ui.bootstrap :slat_actions, slat_header: true

  = ui.bootstrap :slat_list do
    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do

        = ui.bootstrap :slat_item, heading: 'Button version of slat action'
        = ui.bootstrap :slat_item, heading: '$5000', caption: 'An amount caption'

      = ui.bootstrap :slat_actions, button: true, menu: false, icon: 'trash', href: '#nogo'
    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do

        = ui.bootstrap :slat_item, heading: 'Slat information'
        = ui.bootstrap :slat_item, heading: '$5000', caption: 'An amount caption'

      = ui.bootstrap :slat_actions, menu: false, icon: 'trash', href: '#nogo'

    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do

        = ui.bootstrap :slat_item, heading: 'Slat information'
        = ui.bootstrap :slat_item, heading: '$5000', caption: 'An amount caption'

      = ui.bootstrap :slat_actions, menu: false, icon: 'trash', href: '#nogo', method: :get, disable_with: 'Disabler', confirm: 'Are you sure?'

    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do

        = ui.bootstrap :slat_item, heading: 'Button version of slat action'
        = ui.bootstrap :slat_item, heading: '$5000', caption: 'An amount caption'

      = ui.bootstrap :slat_actions, button: true, menu: false, icon: 'trash', href: '#nogo'

.row
  .col-md-6.col-8.mx-auto
    = ui.bootstrap :slats do
      = ui.bootstrap :slat_header do
        = ui.bootstrap :slat do
          = ui.bootstrap :slat_body do
            = ui.bootstrap :slat_item, slat_header: 'First Column'
            = ui.bootstrap :slat_item, slat_header: 'Second Column', class: 'text-right'
          = ui.bootstrap :slat_actions, slat_header: true

      = ui.bootstrap :slat_list do
        = ui.bootstrap :slat do
          = ui.bootstrap :slat_body do

            = ui.bootstrap :slat_item, heading: 'Slat information'
            = ui.bootstrap :slat_item, heading: '$5000', class: 'text-right', caption: 'An amount caption'

          = ui.bootstrap :slat_actions, :danger, menu: false, icon: 'trash', href: '#nogo', confirm: 'Test', method: :delete, wide: false

        = ui.bootstrap :slat do
          = ui.bootstrap :slat_body do

            = ui.bootstrap :slat_item, heading: 'Slat information'
            = ui.bootstrap :slat_item, heading: '$5000', caption: 'An amount caption'

          = ui.bootstrap :slat_actions, menu: false, icon: 'trash', href: '#nogo', method: :get, disable_with: 'Disabler', confirm: 'Are you sure?'



.mt-5
%hr
.mb-5

%h3.my-4 Slat with slat-items lg
= ui.bootstrap :slats do
  = ui.bootstrap :slat_header do
    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do
        = ui.bootstrap :slat_item, slat_header: 'First Column'
        = ui.bootstrap :slat_item, slat_header: 'Second Column'
      = ui.bootstrap :slat_actions, slat_header: true

  = ui.bootstrap :slat_list do
    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do

        = ui.bootstrap :slat_item, :lg, heading: 'Slat information (:lg)'
        = ui.bootstrap :slat_item, heading: '$5000', caption: 'An amount caption'

      = ui.bootstrap :slat_actions, :warning, menu: false, icon: 'trash', href: '#nogo'

    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do

        = ui.bootstrap :slat_item, :lg, heading: 'Slat information (:lg) button version'
        = ui.bootstrap :slat_item, heading: '$5000', caption: 'An amount caption'

      = ui.bootstrap :slat_actions, :warning, menu: false, icon: 'trash', href: '#nogo', button: true

.mt-5
%hr
.mb-5

%h3.my-4 Slat with slat-list sm

= ui.bootstrap :slats do
  = ui.bootstrap :slat_header do
    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do
        = ui.bootstrap :slat_item, slat_header: 'First Column'

  = ui.bootstrap :slat_list, :sm do
    = ui.bootstrap :slat do
      = ui.bootstrap :slat_body do
        = ui.bootstrap :slat_item, heading: 'Your shopping cart'

%hr

%h3.my-4 Slat Actions that build a slat action on their own
.text-right
  ":warning trait (button)"
  %br
  = ui.bootstrap :slat_actions, :warning, menu: false, icon: 'trash', href: '#nogo', button: true
  = ui.bootstrap :slat_actions, :warning, menu: false, icon: 'trash', href: '#nogo', button: true, body: 'Delete'

  %p.my-3
  "Theme: :danger (button)"
  %br
  = ui.bootstrap :slat_actions, theme: :danger, menu: false, icon: 'trash', href: '#nogo', button: true
  = ui.bootstrap :slat_actions, theme: :danger, menu: false, icon: 'trash', href: '#nogo', button: true, body: 'Delete'
  %p.my-3
  "No theme (button)"
  %br
  = ui.bootstrap :slat_actions, menu: false, icon: 'trash', href: '#nogo', button: true
  = ui.bootstrap :slat_actions, menu: false, icon: 'trash', href: '#nogo', button: true, body: 'Delete'
  %p.my-3
  ":warning trait (not button)"
  %br
  = ui.bootstrap :slat_actions, :warning, menu: false, icon: 'trash', href: '#nogo', button: false
  = ui.bootstrap :slat_actions, :warning, menu: false, icon: 'trash', href: '#nogo', button: false, body: 'Delete'
  %p.my-3
  "theme: :danger (not button)"
  %br
  = ui.bootstrap :slat_actions, theme: :danger, menu: false, icon: 'trash', href: '#nogo'
  = ui.bootstrap :slat_actions, theme: :danger, menu: false, icon: 'trash', href: '#nogo', body: 'Delete'

  %p.my-3
  "no theme"
  = ui.bootstrap :slat_actions, menu: false, icon: 'trash', href: '#nogo'
  = ui.bootstrap :slat_actions, menu: false, icon: 'trash', href: '#nogo', body: 'Delete'
