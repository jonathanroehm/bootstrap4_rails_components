%h1 Button Groups

%h2 Pre-configured, design system buttons

%hr

%h2 Manually Created Button Groups

%h3 Patterns

%h4 Button Group Themes
= ui.bootstrap :button_group, class: 'example-class' do
  = ui.bootstrap :button, :primary, as: :button, body: 'Button'
  = ui.bootstrap :button, :primary, as: :button do
    Button
  = ui.bootstrap :button, :primary, as: :button do
    Button

%br

= ui.bootstrap :button_group, class: 'example-class' do
  = ui.bootstrap :button, :secondary, as: :button, body: 'Button'
  = ui.bootstrap :button, :secondary, as: :button do
    Button
  = ui.bootstrap :button, :secondary, as: :button do
    Button

%br

= ui.bootstrap :button_group, class: 'example-class' do
  = ui.bootstrap :button,:success, as: :button, body: 'Button'
  = ui.bootstrap :button,:success, as: :button do
    Button
  = ui.bootstrap :button,:success, as: :button do
    Button

%br

= ui.bootstrap :button_group, class: 'example-class' do
  = ui.bootstrap :button, :danger, as: :button, body: 'Button'
  = ui.bootstrap :button, :danger, as: :button do
    Button
  = ui.bootstrap :button, :danger, as: :button do
    Button

%br

= ui.bootstrap :button_group, class: 'example-class' do
  = ui.bootstrap :button, :warning, as: :button, body: 'Button'
  = ui.bootstrap :button, :warning, as: :button do
    Button
  = ui.bootstrap :button, :warning, as: :button do
    Button

%br

= ui.bootstrap :button_group, class: 'example-class' do
  = ui.bootstrap :button, :info, as: :button, body: 'Button'
  = ui.bootstrap :button, :info, as: :button do
    Button
  = ui.bootstrap :button, :info, as: :button do
    Button

%br

= ui.bootstrap :button_group, class: 'example-class' do
  = ui.bootstrap :button, :light, as: :button, body: 'Button'
  = ui.bootstrap :button, :light, as: :button do
    Button
  = ui.bootstrap :button, :light, as: :button do
    Button

%br

= ui.bootstrap :button_group, class: 'example-class' do
  = ui.bootstrap :button, :dark, as: :button, body: 'Button'
  = ui.bootstrap :button, :dark, as: :button do
    Button
  = ui.bootstrap :button, :dark, as: :button do
    Button


%hr

%h4 Button Groups with links
= ui.bootstrap :button_group, class: 'example-class' do
  / = ui.bootstrap :button, href: '#testURL', body: 'Link Btn W/O :as'
  = ui.bootstrap :button, :link, :active, href: '#testURL' do
    Active Button
  = ui.bootstrap :button, :link do
    Link Button without HREF

%hr

%h4 Button groups with buttons
= ui.bootstrap :button_group, role: 'not a good one' do
  = ui.bootstrap :button, as: :button, body: 'Button'
  = ui.bootstrap :button, as: :button do
    Button
  = ui.bootstrap :button, as: :button do
    Button

%hr

%h3 Sizing

= ui.bootstrap :button_group, :lg, aria: { 'test' => 'test2' } do
  = ui.bootstrap :button, as: :button, body: 'Left'
  = ui.bootstrap :button, as: :button do
    Middle
  = ui.bootstrap :button, as: :button do
    Right

= ui.bootstrap :button_group, aria: { 'test' => 'test2' } do
  = ui.bootstrap :button, as: :button, body: 'Left'
  = ui.bootstrap :button, as: :button do
    Middle
  = ui.bootstrap :button, as: :button do
    Right

= ui.bootstrap :button_group, :sm, aria: { 'test' => 'test2' } do
  = ui.bootstrap :button, as: :button, body: 'Left'
  = ui.bootstrap :button, as: :button do
    Middle
  = ui.bootstrap :button, as: :button do
    Right

%hr

%h4 Nesting Button Groups

%h5 Nesting Sizes
= ui.bootstrap :button_group, :lg, aria: { 'test' => 'test2' } do
  = ui.bootstrap :button, as: :button, body: 'Large Left'
  = ui.bootstrap :button, as: :button do
    Large Middle
  = ui.bootstrap :button, as: :button do
    Large Right

  = ui.bootstrap :button_group, size: :sm, aria: { 'test' => 'test2' } do
    = ui.bootstrap :button, as: :button, body: 'Small Left'
    = ui.bootstrap :button, as: :button do
      Small Middle
    = ui.bootstrap :button, as: :button do
      Small Right

  = ui.bootstrap :button_group, aria: { 'test' => 'test2' } do
    = ui.bootstrap :button, as: :button, body: 'Small Left'
    = ui.bootstrap :button, as: :button do
      Small Middle

%hr

%h3 Vertical variation
%h4 Button groups with buttons
= ui.bootstrap :button_group, :vertical, aria: { 'test' => 'test2' } do
  = ui.bootstrap :button, as: :button, body: 'Button'
  = ui.bootstrap :button, as: :button do
    Button
  = ui.bootstrap :button, as: :button do
    Button
