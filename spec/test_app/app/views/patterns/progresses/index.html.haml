%h1 Progresses

%p
  Pass in the progress attribute as an integer (or a string if you're feeling lucky... it'll convert the number to an integer regardless)
  %br
  %code
    \= ui.bootstrap :progress, progress: 25

= ui.bootstrap :progress, progress: 25, class: 'my-4'
= ui.bootstrap :progress, progress: 50, class: 'my-4'
= ui.bootstrap :progress, progress: 75, class: 'my-4'
= ui.bootstrap :progress, progress: 90, class: 'my-4'
= ui.bootstrap :progress, progress: 100, class: 'my-4', describe: 'Tested data describe', tooltip: 'Tested tooltip'

%hr

%h2 Labels

%p Labels can be applied in 2 flavors when using the singular :progress component. (infinitely flexible if using the :progress > :progress_bar component setup.)
%ol
  %li
    As a boolean (true) on the label attribute which will output the '[:progress]%'
    %br
    %code = ui.bootstrap :progress, progress: 25, label: true
    %br
    %b Label will output 25%
  %li
    As a string
    %br
    %code = ui.bootstrap :progress, label: 'My label'
  %li
    As a built out component with a block passed into progress_bar
    %br
    %code
      %span = ui.bootstrap :progress, class: 'my-4' do
      %br
      %span.ml-3 = ui.bootstrap :progress_bar, progress: 90 do
      %br
      %span.d-inline-block.ml-5 Custom block label

= ui.bootstrap :progress, :label, progress: 60, class: 'my-4'
= ui.bootstrap :progress, progress: 75, class: 'my-4', label: 'Custom string'
= ui.bootstrap :progress, class: 'my-4' do
  = ui.bootstrap :progress_bar, progress: 90 do
    Custom block label

%hr

%h2 Height
%h3 Manually set height
%p leaving the :height option out will use the default heigh value from CSS
= ui.bootstrap :progress, progress: 60, class: 'my-4', height: 1
= ui.bootstrap :progress, progress: 60, class: 'my-4', label: 'No height set'
= ui.bootstrap :progress, progress: 60, class: 'my-4', height: 50, label: 'height: 50'

%hr

%h3 Thematic size
%h4 Traits

= ui.bootstrap :progress, :sm, progress: 60, class: 'my-4'
= ui.bootstrap :progress, :lg, progress: 60, class: 'my-4'

%hr

%h4 Manual
= ui.bootstrap :progress, size: :sm, progress: 60, class: 'my-4'
= ui.bootstrap :progress, size: :lg, progress: 60, class: 'my-4'

%hr

%h2 Themes (backgrounds)
= ui.bootstrap :progress, :primary, progress: 15, class: 'my-4', label: 'Primary'
= ui.bootstrap :progress, :secondary, progress: 25, class: 'my-4', label: 'Secondary'
= ui.bootstrap :progress, :success, progress: 50, class: 'my-4', label: 'Success'
= ui.bootstrap :progress, :warning, progress: 75, class: 'my-4', label: 'Warning'
= ui.bootstrap :progress, :danger, progress: 80, class: 'my-4', label: 'Danger'
= ui.bootstrap :progress, :dark, progress: 90, class: 'my-4', label: 'Dark'
= ui.bootstrap :progress, :light, progress: 95, class: 'my-4'

%hr

%h2 Multiple bars
= ui.bootstrap :progress, class: 'my-4' do
  = ui.bootstrap :progress_bar, :label, progress: 25
  = ui.bootstrap :progress_bar, :success, :label, progress: 25
  = ui.bootstrap :progress_bar, :warning, :label, progress: 25

%hr

%h2 Striped progress bars
= ui.bootstrap :progress, :primary, :striped, progress: 15, class: 'my-4', label: 'Primary Striped'
= ui.bootstrap :progress, :secondary, :striped, progress: 25, class: 'my-4', label: 'Secondary Striped'
= ui.bootstrap :progress, :success, :striped, progress: 50, class: 'my-4', label: 'Success Striped'
= ui.bootstrap :progress, :warning, :striped, progress: 75, class: 'my-4', label: 'Warning Striped'
= ui.bootstrap :progress, :danger, :striped, progress: 80, class: 'my-4', label: 'Danger Striped'
= ui.bootstrap :progress, :dark, :striped, progress: 90, class: 'my-4', label: 'Dark Striped'
= ui.bootstrap :progress, :light, :striped, progress: 95, class: 'my-4'

%hr

%h2 Striped Animated
%p.text-danger Avert your eyes...
%p Note: Animation only "works" when striped is enabled. For thematic purposes, the css class will be added to the component even if striped is not enabled.

= ui.bootstrap :progress, :striped, :animated, :primary, progress: 50, class: 'my-4', label: 'Primary Striped - Animated'

%hr

%h2 Tooltipped
= ui.bootstrap :progress, :label, :warning, class: 'my-4', progress: 80, tooltip: 'A tooltip'
